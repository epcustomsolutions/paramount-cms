.venv/
__pycache__/
*.pyc

# Django
db.sqlite3
*.sqlite3
.DS_Store

# Environment
.env
.env.*

# Static / media
staticfiles/
# static_deploy/ is intentionally not ignored — Vercel build output must be deployable (see build_files.sh).
media/

# Database backups (contain client PII and uploaded documents — never commit)
backups/

# Logs
*.log

